# 🚀 Быстрый старт

## Три команды для запуска

```bash
# 1. Установка
npm install

# 2. Запуск (frontend)
npm run dev

# 3. Открыть http://localhost:3000
```

Готово! 🎉

## Тестовая оплата ЮKassa (по API)

Важно: **секретный ключ ЮKassa нельзя использовать во фронтенде**, поэтому для оплаты нужен локальный сервер.

```bash
# 1) Создайте .env
cp .env.example .env

# 2) Запустите backend (в отдельном терминале)
npm run dev:server

# 3) Запустите frontend (как обычно)
npm run dev
```

Далее: выберите изображение → введите промпт → нажмите **«Оживить момент»** → произойдёт редирект на тестовую оплату ЮKassa.

## Что дальше?

### Разработка
- Редактируйте файлы в `src/`
- Изменения применяются мгновенно (hot reload)
- TypeScript подсвечивает ошибки

### Структура кода
```
src/
├── app/              # Настройка приложения
├── widgets/          # Большие блоки (MainScreen)
├── entities/         # Бизнес-логика (image-upload)
└── shared/           # Общий код (ui, types, lib)
```

### Основные файлы
- `src/app/App.tsx` - главный компонент
- `src/widgets/main-screen/ui/MainScreen.tsx` - главный экран
- `src/shared/config/theme.ts` - цвета и стили

### Полезные команды
```bash
npm run build    # Сборка для production
npm run preview  # Просмотр production
npm run lint     # Проверка кода
```

### Добавление нового компонента

Пример создания кнопки:

```typescript
// src/shared/ui/MyButton/MyButton.tsx
import styled from '@emotion/styled';
import { FC } from 'react';

const StyledButton = styled.button`
  background: #2079cc;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 8px;
`;

export const MyButton: FC = () => {
  return <StyledButton>Кнопка</StyledButton>;
};
```

### Изменение дизайна

Цвета в `src/shared/config/theme.ts`:

```typescript
export const theme = {
  colors: {
    background: '#0E1116',  // Фон
    primary: '#011F66',     // Основной цвет
    secondary: '#2079CC',   // Дополнительный
    white: '#FFFFFF',       // Белый
  },
};
```

### Telegram Mini App

Код для Telegram уже настроен:
- Автоматическая инициализация
- Настройка цветов
- Работа с файлами

Проверьте `src/shared/lib/telegram.ts`

## Проблемы?

### Изображения не показываются
```bash
cp images/*.svg images/*.png public/images/
```

### Порт занят
```bash
npm run dev -- --port 4000
```

### Ошибки установки
```bash
rm -rf node_modules package-lock.json
npm install
```

## Документация

- 📖 [README.md](README.md) - общая информация
- 🔧 [INSTALLATION.md](INSTALLATION.md) - подробная установка
- 🔄 [MIGRATION.md](MIGRATION.md) - что изменилось

## Технологии

- ⚛️ React 18 - UI
- 📘 TypeScript - типы
- 💅 Emotion - стили
- ⚡ Vite - сборка
- 🏗️ FSD - архитектура

---

**Готово к разработке!** 🎨
